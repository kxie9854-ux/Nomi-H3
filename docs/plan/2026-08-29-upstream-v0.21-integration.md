# Nomi-H3 整合 upstream v0.21.0

状态：🧊 暂缓/远期 — 冻结在 v0.21 基线 `4cac784e`。本 fork 不再暗示跟踪 `origin/main`。

日期：2026-08-29

## 目标

以 `upstream/main` (`e2f274bc`, Nomi v0.21.0) 为新基线，迁移 Nomi-H3 从 v0.20.1 后增加的 AutoDL.art MiniMax H3、内嵌 Codex 导演、项目级会话、Skill 点选、多镜定妆、BGM/时间线/导出闭环与相关修复。

## 整合方法

1. 从共同祖先 `ae53045b` 识别 fork 自有提交。
2. 以最新 `upstream/main` 为新基线，按原顺序重放 fork 提交。
3. 对 43 个双方重叠文件逐项对账；上游已有更通用实现时，保留上游语义，只接入 AutoDL.art/Codex 所需的差异。
4. 不保留平行旧入口、fallback 或两套时间线采纳逻辑。

## 范围

- `electron/capabilityCore/`：H3 模式、MCP 工具、生成恢复、分组、冻结、时间线组装/导出。
- `electron/codexAppServer/` 与 `electron/ai/`：内嵌 Codex 导演、历史恢复、文本模型与 Skill 管理。
- `electron/catalog/`、`catalog/` 与 `src/config/modelArchetypes/`：AutoDL.art H3 和 Codex 本地模型档案。
- `src/workbench/`：导演面板、画布恢复、定妆引用、音频、时间线与成片节点。
- `skills/`：H3 导演与官方提示词结构。
- 相关单测、真机走查脚本和 fork 交接文档。

## 不动项

- 不把 AutoDL.art 密钥、ChatGPT 登录态或本机项目数据提交进 Git。
- 不伪造 AutoDL.art 不存在的单首帧 I2VA 或参考视频槽。
- 不恢复 Dreamina 静帧或已禁用的 playbook 平行制作线。
- 不修改现有用户的 `.tmp/electron-user-data/` 项目与密钥。

## 回滚

整合只在 `codex/integrate-upstream-0.21` 上进行。回滚时删除该分支即可；原 `main` 与现有本机项目数据不受影响。

## 验收门

1. fork 定向单测、上游受影响单测全过。
2. `check:filesize` / `check:tokens` / `check:i18n` / `lint:ci` / `typecheck` / `test` / `build` 全过。
3. 真实桌面走查：空项目打开导演 → 简报 → 首尾静帧 → H3 视频 → 时间线 → MP4 导出。
4. 多镜走查：定妆卡冻结后，各镜静帧沿参考边走 Codex image edit，项目重启后导演会话与画布摘要恢复。
5. 确认上游 v0.21.0 的批量制作、受控采纳、新模型目录和 Electron 43 能力未被 fork 回退。
6. Vite + Electron 开发态可正常进入项目库；renderer 不跨层加载 Node/XML 上传实现。
